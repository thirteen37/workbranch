{
  "description": "Workbranch hooks: Soft-block commits on main/master branch, suggest using worktree workflow",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if this Bash command is a git commit on main/master branch. Command: $TOOL_INPUT\n\nIf this is a git commit command AND on main/master branch, return 'ask' with a message suggesting worktree workflow. Otherwise return 'allow'.",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
